/* Global */
* {
    font-family: "JetBrainsMono Nerd Font";
    font-size: 13px;
    color: #cdd6f4;
}

/* Bar background */
window#waybar {
    background: rgba(30, 30, 46, 0.9);
    border-bottom: 2px solid #cba6f7;
}

/* Workspaces */
#workspaces button {
    padding: 0 8px;
    color: #a6adc8;
}

#workspaces button.active {
    color: #cba6f7;
    border-bottom: 2px solid #cba6f7;
}

/* Clock */
#clock {
    color: #f5c2e7;
}

/* Right modules spacing + subtle grouping */
#cpu, #memory, #network, #pulseaudio {
    padding: 0 10px;
    margin: 0 2px;
    border-radius: 6px;
    background: rgba(49, 50, 68, 0.5);
}

/* Optional: give each module a slight personality */
#cpu {
    color: #fab387;
}

#memory {
    color: #94e2d5;
}

#network {
    color: #89b4fa;
}

#pulseaudio {
    color: #f9e2af;
}
